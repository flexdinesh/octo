# TanStack Start Nitro Web App

We use TanStack Start for the `web` workspace and Nitro's default Node output for the production server. This keeps the scaffold minimal while preserving server-side rendering and a production `start` command without committing to a provider-specific preset.
